/** Auto-highlight eases out over this window before `expiresAt`. */
export const HIGHLIGHT_AUTO_FADE_MS = 2800;

/** Pinned canvas blobs fade out over this duration, then the pin is removed. */
export const PINNED_HIGHLIGHT_MS = 12_000;
